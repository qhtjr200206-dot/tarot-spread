# 타로 스프레드 아카이브 — 기획서 (PLAN.md)

> 이 문서는 "무엇을 만들 것인가"를 정의합니다. "어떻게 실패 없이 만들 것인가"는 `BUILD_GUIDE.md`를 참고하세요.

## 1. 한 줄 정의

관리자가 **타로 스프레드(질문 + 카드 자리별 의미 구성)**를 만들고, 그 스프레드로 **날짜별 리딩(실제로 뽑은 카드와 해석)**을 기록해 나가면, 누구나 방문해서 과거 리딩 내용을 열람할 수 있는 정적 아카이브 사이트.

## 2. 사용자 유형

| 유형 | 권한 | 인증 방식 |
|---|---|---|
| 일반 방문자 | 스프레드/리딩 열람만 가능 | 없음 (누구나) |
| 관리자 (리더 본인) | 스프레드 생성, 리딩 추가/기록 | GitHub Personal Access Token(PAT) 직접 입력 |

- 관리자 인증은 별도 백엔드 없이, 브라우저에서 GitHub PAT를 세션에 보관하고 GitHub REST API(Contents API)를 직접 호출해 저장소에 커밋하는 방식입니다. 즉 "이 저장소에 쓰기 권한이 있는 사람 = 관리자"입니다.
- PAT는 `sessionStorage`에만 저장하고 절대 저장소 파일이나 로그로 남기지 않습니다.

## 3. 핵심 개념 및 용어

- **스프레드(Spread)**: 하나의 "질문 템플릿". 예) "이 인물은 어떤 성격인가?"
  - 질문 자체가 기본 표시명이 되며, 관리자가 별도의 스프레드 이름을 지정할 수도 있음 (예: "성격 분석 스프레드").
  - N개의 **포지션(자리)**으로 구성됨. 각 포지션은 순서(1, 2, 3…)와 그 자리의 의미로 이루어짐.
    - 예) 1번 자리 = "성격", 2번 자리 = "성격이 형성된 배경"
  - 스프레드는 "설계도"일 뿐, 실제로 뽑은 카드는 포함하지 않음.
- **리딩(Reading)**: 특정 스프레드를 가지고 **특정 날짜에 실제로 진행한 한 번의 타로 점**.
  - 하나의 스프레드에는 여러 개의 리딩이 날짜별로 쌓일 수 있음.
  - 각 리딩은 포지션별로 실제 뽑힌 카드, 그 자리의 의미, AI 해석, 그리고 전체 총평/리더의 개인 해석을 담음.

## 4. 정보 구조 (사이트맵)

```
/                             홈 — 스프레드 목록
/spread.html?id=xxx           스프레드 상세 — 포지션 구성 안내 + 리딩(날짜) 목록
/reading.html?spread=xxx&id=yyy   리딩 상세 — 카드/해석 전체 열람
/admin/login.html             관리자 로그인 (PAT 입력)
/admin/spread-new.html        새 스프레드 만들기 (관리자 전용)
/admin/reading-new.html?spread=xxx   새 리딩 추가 (관리자 전용)
```

## 5. 페이지별 요구사항

### 5.1 홈 (`/`)
- 등록된 모든 스프레드를 카드 목록 형태로 표시: 표시명(질문 또는 커스텀 이름), 포지션 개수, 누적 리딩 수, 최근 리딩 날짜.
- 클릭 시 해당 스프레드 상세로 이동.
- 우측 상단: 다크/라이트 토글, 관리자 로그인 상태 표시.
- 로그인 상태(관리자)일 때만 "+ 새 스프레드" 버튼 노출.

### 5.2 스프레드 상세 (`/spread.html?id=`)
- 스프레드 질문/이름, 설명, 포지션별 순서와 의미를 표로 표시 (예: 1번 - 성격, 2번 - 형성 배경 …).
- 이 스프레드로 진행된 리딩을 날짜 내림차순 목록으로 표시.
- 관리자 로그인 상태일 때만 "+ 리딩 추가" 버튼 노출.

### 5.3 리딩 상세 (`/reading.html?spread=&id=`)
- 리딩 날짜, (선택) 리딩 제목.
- 포지션별 표: 순서 / 포지션 의미 / 실제 뽑힌 카드 / 그 자리에 대한 AI 해석.
- **전체 총합 해석**: 모든 카드를 종합한 해석 (AI 생성 + 관리자가 다듬을 수 있음).
- **리더의 개인적 해석**: 관리자가 직접 작성한 자유 텍스트.
- 방문자는 읽기만 가능, 로그인 불필요.

### 5.4 관리자 — 로그인 (`/admin/login.html`)
- GitHub PAT 입력 폼 (repo 쓰기 권한이 있는 fine-grained 또는 classic PAT).
- 입력 즉시 GitHub API로 토큰 유효성 + 해당 저장소 쓰기 권한을 검증 후 세션에 저장.
- 실패 시 명확한 오류 메시지 (토큰 무효 / 권한 부족 / 저장소 접근 불가 구분).

### 5.5 관리자 — 새 스프레드 (`/admin/spread-new.html`)
- 질문(필수, 기본 표시명) 입력.
- 커스텀 이름(선택) 입력.
- 포지션을 원하는 개수만큼 동적으로 추가/삭제하며 "순서"와 "의미"를 입력.
- 저장 시 GitHub Contents API로 `data/spreads/{id}.json` 생성 + `data/spreads/index.json` 갱신 커밋.

### 5.6 관리자 — 새 리딩 추가 (`/admin/reading-new.html?spread=`)
- 리딩 날짜 선택(달력), 리딩 제목(선택).
- 해당 스프레드의 포지션 목록이 자동으로 표시되고, 각 포지션에 뽑힌 카드명을 입력(오탈자 방지를 위해 78장 카드 목록에서 자동완성/선택).
- "AI 해석 생성" 버튼 1회 클릭 → 카드+포지션 의미 전체를 모아 Gemini API를 **한 번만** 호출 → 포지션별 해석 + 총합 해석 초안을 받아와 폼에 채움 (관리자가 수정 가능).
- 리더의 개인적 해석 자유 입력란.
- 저장 시 `data/readings/{spreadId}/{readingId}.json` 생성 + `data/readings/{spreadId}/index.json` 갱신 커밋.

## 6. 데이터 모델

모든 콘텐츠는 저장소 내 정적 JSON 파일로 관리합니다 (별도 DB 없음).

```
data/
  spreads/
    index.json                # 전체 스프레드 목록(요약) — 홈 화면용
    {spreadId}.json           # 스프레드 상세(포지션 정의)
  readings/
    {spreadId}/
      index.json               # 해당 스프레드의 리딩 목록(요약, 날짜순)
      {readingId}.json         # 리딩 상세 전체 내용
```

`data/spreads/{spreadId}.json`
```json
{
  "id": "spread_20260809_personality",
  "question": "이 인물은 어떤 성격인가?",
  "customName": "성격 분석 스프레드",
  "description": "인물의 성격과 그 배경을 파악하기 위한 3카드 스프레드",
  "positions": [
    { "order": 1, "label": "성격", "meaning": "인물의 표면적으로 드러나는 성격" },
    { "order": 2, "label": "형성 배경", "meaning": "그 성격이 형성된 배경이나 원인" },
    { "order": 3, "label": "발현 방식", "meaning": "그 성격이 실제로 드러나는 방식" }
  ],
  "createdAt": "2026-08-09T10:00:00+09:00",
  "createdBy": "github-username"
}
```

`data/readings/{spreadId}/{readingId}.json`
```json
{
  "id": "reading_20260809",
  "spreadId": "spread_20260809_personality",
  "date": "2026-08-09",
  "title": null,
  "characterContext": {
    "name": "드미트리 카라마조프",
    "info": "퇴역 장교, 격정적이고 충동적인 성격",
    "situation": "아버지와 유산 문제로 갈등하는 시점"
  },
  "cards": [
    {
      "order": 1,
      "card": "The Fool",
      "orientation": "upright",
      "positionLabel": "성격",
      "positionMeaning": "인물의 표면적으로 드러나는 성격",
      "aiInterpretation": "..."
    }
  ],
  "overallInterpretation": "...",
  "readerNote": "...",
  "createdAt": "2026-08-09T21:00:00+09:00",
  "createdBy": "github-username"
}
```

- `characterContext`는 선택 항목입니다(캐릭터 이름/기본 정보/상황을 입력하면 AI 해석이 그 인물에 맞춰 생성되고, 비워두면 `null`).
- `orientation`은 `"upright"`(정방향) 또는 `"reversed"`(역방향)입니다.

## 7. Gemini 연동 설계 (요약)

- 브라우저(GitHub Pages)에서 Gemini API를 직접 호출하지 않습니다 — API 키가 그대로 노출되기 때문입니다.
- Cloudflare Worker 프록시를 처음 시도했으나, Google Gemini API가 특정 지역을 차단하고 Cloudflare Workers는 무료 플랜에서 실행 지역을 고정할 수 없어(Enterprise 전용 기능) 요청이 계속 지역 차단에 걸리는 문제가 있었습니다. 그래서 **GitHub Actions**로 전환했습니다.
- 흐름: 관리자 화면 → `data/_requests/{id}.json` 커밋(카드+포지션+캐릭터 정보) → GitHub Actions(`gemini-interpret.yml`)가 push를 감지해 repo secret으로 Gemini 호출 → 78장 카드 참고 의미표(`.github/scripts/card-meanings.mjs`)를 프롬프트에 포함 → 결과를 `data/_responses/{id}.json`으로 커밋 → 관리자 화면이 그 파일이 나타날 때까지 폴링 후 폼에 채움 → 관리자가 검토/수정 후 저장.
- 이 방식은 완전 무료지만, GitHub Actions 실행 시간만큼(보통 수십 초~2분) 결과 반영이 지연됩니다.
- 리딩 1건당 Gemini 호출은 정확히 1회 (모든 카드+포지션+캐릭터 정보를 한 번의 프롬프트로 묶어서 전송). "다시 생성" 버튼으로 재호출 가능.
- 상세 구현 순서는 `BUILD_GUIDE.md`의 4단계를 참고.

## 8. 디자인 가이드

### 8.1 테마
- 라이트 모드와 다크 모드를 모두 지원하고, 두 모드 모두 **보라색 계열을 포인트 색상**으로 사용.
- 사용자가 명시적으로 전환하지 않으면 시스템 설정(`prefers-color-scheme`)을 따르고, 전환한 선택은 `localStorage`에 저장해 유지.

### 8.2 컬러 토큰 (권장 기본값, CSS 커스텀 프로퍼티로 관리)

| 토큰 | 라이트 | 다크 |
|---|---|---|
| `--bg` | `#faf9fc` | `#14121f` |
| `--surface` | `#ffffff` | `#1e1b2e` |
| `--text` | `#1e1b2e` | `#f3f1fa` |
| `--text-muted` | `#6b6580` | `#a79fc2` |
| `--border` | `#e4e0f0` | `#322d4a` |
| `--accent` | `#7c3aed` | `#a78bfa` |
| `--accent-hover` | `#6d28d9` | `#c4b5fd` |

### 8.3 톤 & 무드
- 신비롭지만 과하지 않게: 보라색은 포인트로만 사용(버튼, 링크, 강조 태그, 헤더 로고)하고 본문 배경은 차분한 무채색 계열 유지.
- 카드/리딩 목록은 타로 카드를 연상시키는 라운드 코너 + 은은한 그림자 카드 UI 권장.

## 9. 범위 및 향후 확장 (MVP 밖)

**MVP 포함**
- 스프레드 CRUD(생성/열람), 리딩 CRUD(생성/열람), Gemini 1회 해석, 다크/라이트 토글, 관리자 인증.

**MVP 밖 (필요 시 추후 검토)**
- 리딩/스프레드 수정·삭제 UI (초기에는 잘못 기록 시 관리자가 GitHub에서 직접 커밋 수정하는 것으로 대체 가능).
- 카드 정방향/역방향 구분, 카드 이미지 표시.
- 검색/태그/필터, 카드별 통계(어떤 카드가 자주 나왔는지 등).
- 다중 관리자 권한 분리, 댓글/공유 기능.

## 10. 성공 기준

- GitHub Pages URL만으로 방문자가 모든 스프레드와 과거 리딩을 열람할 수 있다.
- 관리자가 다른 기기/브라우저에서도 PAT만 있으면 콘텐츠를 추가할 수 있다.
- Gemini API 키가 클라이언트 코드나 저장소 어디에도 평문으로 존재하지 않는다.
- 리딩 1건 기록 시 Gemini 호출이 정확히 1회만 발생한다.
